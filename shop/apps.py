from django.apps import AppConfig


class ShopConfig(AppConfig):
    name = 'shop'
    verbose_name = 'Shop'
    #make the cod as another option and also when user selected as online payment after all the card details given i should show a success message witha green tick and redirecting to user dashboard for orders listing of the user.alkso in the book a portrait booking form it should ask for size a4,a3,chart size,categories having drop downlist for selecting and the uploaded image should be downlodable and visible for admin in the admin dashboard,also the ctalogue manager should firstly the admin should select add new category or add produt in a category which is already existing and based on that CRUD operations should be donw ,also with the image which should reflect in the user UI ,currently image is not displayed,also when the product is viewed it should be viewed with many images of one product using slidewshow automatic slideshow,also in the image upload of all products by the admin,it should upload morw than one image or one image based on admin but slideshow when clicked view is  a must ,also enhance the designs of the overall website 






